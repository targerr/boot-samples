<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.enterprise.dao.mapper.BlogMapper">

    <select id="getList" resultType="com.example.enterprise.entity.Blog">
        select * from blog
        <where>
            ${ew.sqlSegment}
        </where>

    </select>
    <select id="selectAllCustomer" resultType="com.example.enterprise.entity.Blog">
        select * from blog
    </select>
</mapper>
